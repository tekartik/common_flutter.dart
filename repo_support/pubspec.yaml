name: tekartik_common_flutter_repo_support
version: 0.1.0
publish_to: none
deploy_to: none

environment:
  sdk: ^3.12.0

dependencies:
  process_run: ">=1.2.1+1"

dev_dependencies:
  path: ">=1.9.1"
  test: ">=1.31.0"
  dev_build: ">=1.1.7+3"
  tekartik_lints:
    git:
      url: https://github.com/tekartik/common.dart
      path: packages/lints
    version: '>=0.1.0'
resolution: workspace

